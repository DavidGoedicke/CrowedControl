/// <summary>
/// using general return type int as an error return message 0=fail    1=success
/// </summary>


using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class agent : MonoBehaviour {
	/*
	goal mainGoal; // each agent has one main goals 
	List<goal> subgoals; // a few subgoals to reach the main goal
	double alivetime;
	int age;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}


	void newBelieve(believe newInput){
		if (mainGoal.relevant(newInput) == 1) {
			bool success=false;
		
			foreach (goal g in subgoals) {


				if (g.relevant(newInput) == 2) {
					g.addBelieve(newInput);
					success = true;
					break;
				}
					
			}


		
		
		}


	}
*/
}
